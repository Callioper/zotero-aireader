# Zotero AI Reader 插件修复操作指南

## 任务目标
修复插件右键菜单不显示的问题（菜单 ID 从 `item-context-menu` 改为 `zotero-itemmenu`），并通过 GitHub Actions 生成正确构建。

## 已完成的修改
1. `src/hooks.ts` - 已修复菜单 ID
2. `addon/bootstrap.js` - 已修复 `__addonInstance__` 占位符

## 待操作

### 1. 初始化 Git 并推送
在项目目录 `C:\Users\Eclaw\Downloads\opencode-transfer\ai-reader-zotero-plugin` 打开终端，执行：

```bash
git add .
git commit -m "fix: change menu ID from item-context-menu to zotero-itemmenu"
git branch -M main
git remote add origin https://github.com/Callioper/zotero-aireader.git
git push -u origin main
```

### 2. 创建标签触发 GitHub Actions
```bash
git tag v1.4.3
git push origin v1.4.3
```

### 3. 下载构建的 XPI
访问 https://github.com/Callioper/zotero-aireader/releases ，下载 `zotero-ai-reader.xpi`

### 4. 测试插件
1. 打开 Zotero → Tools → Add-ons
2. 卸载旧版本
3. Tools → Developer → Load Addon → 选择下载的 XPI
4. 重启 Zotero
5. 右键点击条目，应该看到 "AI Reader" 菜单

## 预期结果
GitHub Actions 会自动构建，生成的 XPI 应该：
- 版本号：1.4.3
- 右键菜单正常显示
- 设置页面正常加载
